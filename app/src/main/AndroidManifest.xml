<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReportesCINERGIA"
        tools:targetApi="31">
        <activity
            android:name=".EnviarActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.actividadesActivity"
            android:exported="false" />
        <activity
            android:name=".fotos.fotosTresActivity"
            android:exported="false" />
        <activity
            android:name=".fotos.fotoDosActivity"
            android:exported="false" />
        <activity
            android:name=".fotos.fotosUnoActivity"
            android:exported="false" />
        <activity
            android:name=".datos.datosActivity"
            android:exported="false" />
        <activity
            android:name=".Generador.ReporteActivity"
            android:exported="false" />
        <activity
            android:name=".MainReportes"
            android:exported="false" />
        <activity
            android:name=".Inicio"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ReportesCINERGIA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.cinergia.cinercia.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>